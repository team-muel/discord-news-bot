FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir "litellm[proxy]==1.82.2"

COPY litellm.config.yaml /app/litellm.config.yaml

EXPOSE 4000

CMD ["litellm", "--config", "/app/litellm.config.yaml", "--host", "0.0.0.0", "--port", "4000"]
